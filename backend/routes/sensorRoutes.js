const express = require('express');
const router = express.Router();
const sensorController = require('../controllers/sensorController');

// Define routes
// Example: router.get('/', sensorController.getAllSensors);

module.exports = router;
